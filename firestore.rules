rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary development rule: allow all reads and writes.
    // WARNING: This is insecure for production use.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
